 /* import React from 'react'

export default function RecentSearches(props){
  
    const searchHistory = []
    
    const cat = JSON.parse(localStorage.getItem('search'))
    
    searchHistory.push(cat)

    console.log(searchHistory)

    const searchDisplay = searchHistory.map(x => <div><h1>{x.what}</h1><h1>{x.where}</h1></div>)
  
  return (
    <div>

     {searchDisplay}

    </div>
  )
} */

